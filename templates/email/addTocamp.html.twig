<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<title>
			{% block title %}Ajout à la campagne de dépistage {{ location }} {% endblock %}
		</title>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
		<link rel="icon" type="image/png" sizes="32x32" href="../assets/flavicon/favicon-32x32.png">
	</head>
	<body data-instant-allow-query-string data-instant-allow-external-links>
    <style>
        .example-wrapper { margin: 1em auto; max-width: 1080px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <p> Salutation {{ user.userName }} .</p>
        <p> Tu as été ajoutée à la campagne de dépistage à  {{ location }} </p>
        {% if user.token is defined %}
            <p> Et Bienvenue a CitiTest </p>
            <p> Ton login: {{ user.userName }} </p>
            <p> Ton mot de passe: {{ user.passwd }}</p>
            <a href={{ "https://citi-test.xyz/addUser/" ~ user.token }}> Ce Lien te permet d'activer ton compte et changer ton mot de passe si désiré.</a>
        {% endif %}
    </div>
	</body>
</html>